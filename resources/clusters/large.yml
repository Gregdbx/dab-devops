resources:
  clusters:
    large_cluster:
      cluster_name: ${var.env}_large_cluster
      node_type_id: Standard_DS5_v2
      num_workers: 4
      spark_version: 14.3.x-scala2.12
      autotermination_minutes: 120
      policy_id: ${var.policy_id}