resources:
  clusters:
    small_cluster:
      cluster_name: ${var.env}_smallcluster
      node_type_id: Standard_DS3_v2
      num_workers: 1
      spark_version: 14.3.x-scala2.12
      autotermination_minutes: 120
      policy_id: ${var.policy_id}